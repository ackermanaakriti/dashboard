import React from 'react'
import { useLayouData } from '../Context/MainLayoutContext'

const MenustopTab = () => {
    const {menuTab,setmenuTab} = useLayouData();
    console.log(menuTab)
  return (
   <>
   </>
  )
}

export default MenustopTab